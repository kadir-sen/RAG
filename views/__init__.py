"""Page modules for the Streamlit UI — one module per page.

The package is named views/ (not pages/) deliberately: Streamlit gives
a literal pages/ directory magic auto-navigation meaning, which would
render a file-based nav on the access-gate screen before
st.navigation() runs.
"""
